# Travel-AI 智能旅游推荐平台项目书（基于现有工程）

## 一、项目基本信息
- 项目名称：Travel-AI 智能旅游推荐平台
- 项目类型：Web 应用 + 智能推荐系统 + 知识图谱增强问答
- 建设形态：前后端分离 + 算法服务独立部署 + 文件服务独立部署
- 适用场景：旅游景点信息管理、用户行为分析、个性化推荐、智能问答导览

## 二、项目背景与建设意义
随着文旅消费场景数字化，传统旅游平台在“发现效率”和“个性化体验”方面存在明显不足：
1. 用户难以在海量景点中快速找到符合偏好的内容。
2. 推荐能力常停留在简单热门排序，缺少语义理解与关系推理。
3. 平台数据（用户、景点、互动行为）未形成统一知识网络，难以支撑深度智能服务。

本项目通过“业务系统 + 知识图谱 + 大模型”融合，实现从“信息展示平台”向“智能决策助手”升级，提升用户决策效率与平台转化潜力。

## 三、项目目标
### 3.1 总体目标
建设一个集“内容管理、用户互动、智能推荐、智能问答、图谱可视化”于一体的旅游智能平台。

### 3.2 分目标
1. 完成旅游内容全流程管理：分类、景点、文件、评论、点赞、收藏、行为记录。
2. 建立用户行为驱动的个性化推荐能力（协同过滤 + 内容推荐）。
3. 建立知识图谱并实现 MySQL 到 Neo4j 的数据同步。
4. 接入 LLM，实现自然语言问答与基于聊天上下文的景点推荐。
5. 提供可视化管理与健康检查能力，支持系统运维与算法效果观察。

## 四、项目范围
### 4.1 建设范围（In Scope）
1. 前端用户端与管理端页面建设（Vue3 + TypeScript）。
2. 业务后端接口建设（Spring Boot + MyBatis Plus + MySQL）。
3. 算法服务建设（Flask + Neo4j + LLM）。
4. 文件服务接入（图片与附件存储访问）。
5. 一键启动脚本与本地化联调环境。

### 4.2 非建设范围（Out of Scope）
1. 多租户隔离与企业级权限域管理。
2. 大规模分布式部署与自动弹性扩容。
3. 商业支付网关、订单履约与财务结算闭环。

## 五、需求分析
### 5.1 角色与核心诉求
1. 普通用户：浏览景点、搜索与筛选、点赞收藏评论、查看推荐、智能对话咨询。
2. 管理员：用户管理、分类管理、景点内容管理、系统监控。
3. 平台运营：分析用户行为，持续优化推荐质量与内容供给。

### 5.2 功能需求
1. 用户与权限：注册登录、JWT 鉴权、角色区分（管理员/普通用户）。
2. 内容管理：分类管理、景点管理、标签维护、封面与文件上传。
3. 互动能力：评论（含回复层级）、点赞、收藏、行为日志（浏览/预约）。
4. 推荐能力：
   - 基于用户的协同过滤推荐。
   - 基于内容的相似推荐。
   - 基于聊天语义关键词的图谱匹配推荐。
5. 知识图谱能力：
   - 数据同步（关系构建）。
   - 图谱统计与可视化。
   - 用户画像与关系分析接口。
6. 智能问答：多模型可选、普通对话、图谱增强问答、对话驱动推荐。

### 5.3 非功能需求
1. 可用性：前后端与算法服务解耦，单模块故障可隔离。
2. 可维护性：模块化目录结构，接口职责清晰。
3. 可扩展性：可新增推荐算法、替换大模型、扩展图谱实体关系。
4. 安全性：JWT、内部服务 token、基础参数校验。
5. 性能：关键查询走索引，图谱查询限制返回量。

## 六、总体技术方案
### 6.1 技术栈
1. 前端：Vue3、TypeScript、Vite、Pinia、Vue Router、Element Plus、ECharts。
2. 业务后端：Spring Boot 3、MyBatis Plus、MySQL、Swagger/OpenAPI、JWT。
3. 算法后端：Flask、Neo4j、Requests、LLM 客户端封装。
4. 文件服务：独立文件服务进程，提供头像/图标/封面/附件存储能力。

### 6.2 系统架构
采用“前端网关层 + 业务服务层 + 智能算法层 + 数据存储层”的分层架构：
1. 前端层：承载用户交互、管理端操作、图表展示与可视化。
2. 业务层（Spring Boot）：处理核心业务规则与关系型数据读写。
3. 智能层（Flask）：处理推荐算法、图谱同步/查询、LLM 对话能力。
4. 存储层：MySQL 保存业务数据，Neo4j 保存图谱关系，文件服务保存静态资源。

## 七、数据设计（摘要）
### 7.1 关系型核心表
1. user：用户信息、角色、状态。
2. category：旅游类别。
3. item：景点主体信息（含标签与扩展 JSON）。
4. item_like：点赞关系。
5. favorite：收藏关系。
6. comment：评论与回复结构。
7. user_action：行为日志（浏览/预约）。

### 7.2 图谱模型（抽象）
1. 节点：User、Item、Category、Tag。
2. 关系：CREATED、BELONGS_TO、HAS_TAG、VIEWED、LIKED、FAVORITED、PURCHASED。
3. 用途：语义检索、用户兴趣推断、关联推荐、图谱可视化。

## 八、接口与模块设计（摘要）
### 8.1 业务后端模块
1. 用户模块：认证、用户资料、权限校验。
2. 景点模块：景点 CRUD、分类归属、文件关联。
3. 互动模块：点赞、收藏、评论、行为记录。
4. 对外集成：调用算法服务与文件服务。

### 8.2 算法服务模块
1. 健康检查：服务可达性与状态检查。
2. 知识图谱：数据同步、统计、可视化、画像接口。
3. 推荐系统：用户协同过滤与内容推荐。
4. LLM 模块：模型列表、普通对话、GraphRAG 对话、聊天推荐。

### 8.3 前端模块
1. 用户端：首页、分类页、景点详情、历史与个人中心、智能聊天。
2. 管理端：用户管理、分类管理、景点管理、算法健康检查。
3. 公共组件：分页、评论列表、地图组件、点赞收藏组件、聊天组件。

## 九、部署与运行方案
### 9.1 本地启动方式
使用一键脚本依次启动：
1. 前端服务（Vite）。
2. Spring Boot 业务服务。
3. Flask 算法服务（Conda 环境）。
4. 文件服务进程。
5. Neo4j 图数据库。

### 9.2 默认端口与服务关系
1. 前端：5173。
2. 业务后端：8080（统一 /api 上下文）。
3. 算法服务：5000（由业务后端进行服务间调用）。
4. 文件服务：5001。
5. 数据库：MySQL + Neo4j。

## 十、测试与验收标准
### 10.1 功能验收
1. 用户注册/登录/鉴权流程完整可用。
2. 分类与景点增删改查正常。
3. 点赞/收藏/评论/行为记录可追踪。
4. 推荐接口返回与用户偏好相关的结果。
5. LLM 对话与聊天推荐接口可用。
6. 图谱同步后可查看统计和可视化结果。

### 10.2 技术验收
1. 模块边界清晰，接口文档可访问。
2. 核心 API 错误码和响应结构统一。
3. 启动脚本可在标准开发机完成联调。
4. 关键配置项可外部化管理。

## 十一、项目计划（建议）
1. 第 1 周：需求确认、架构冻结、环境初始化。
2. 第 2-3 周：业务后端与数据库实现。
3. 第 3-4 周：前端核心页面与交互实现。
4. 第 4-5 周：算法服务、图谱同步与推荐能力实现。
5. 第 5-6 周：联调测试、性能优化、文档与答辩材料整理。

## 十二、风险与应对
1. 风险：多服务联调复杂，依赖多。
   - 应对：统一启动脚本、固定端口与健康检查。
2. 风险：图谱数据质量影响推荐效果。
   - 应对：建立同步校验与统计检查机制。
3. 风险：LLM 输出不稳定。
   - 应对：关键词提取兜底策略（历史交互推荐回退）。
4. 风险：安全配置薄弱（默认密码/密钥）。
   - 应对：部署前完成密钥替换、密码加固与最小权限配置。

## 十三、创新点与特色
1. 业务数据与图谱数据双模融合，增强推荐解释性。
2. 聊天语义驱动的推荐链路，支持自然语言检索景点。
3. 推荐结果具备回退策略，提高在低命中场景下的可用性。
4. 支持图谱可视化，便于教学演示与运营分析。

## 十四、预期成果
1. 可运行的旅游智能平台（前端、业务后端、算法后端、文件服务）。
2. 完整数据库初始化脚本与样例数据。
3. 推荐与智能问答能力的可演示链路。
4. 项目文档：项目书、接口文档、部署说明、测试记录。

## 十五、后续迭代建议
1. 引入向量检索与混合召回，提升冷启动与长尾效果。
2. 增加推荐效果评估指标体系（CTR、转化率、覆盖率、多样性）。
3. 建立数据治理流程（标签标准化、脏数据清洗、质量看板）。
4. 增加运营策略模块（专题推荐、节假日场景化推荐）。

---
说明：本项目书基于现有工程结构和代码能力整理，适用于课程设计、创新创业项目申报、阶段性结题材料。若你有指定“学校/单位模板”，可在此文档基础上快速改造成对应格式。